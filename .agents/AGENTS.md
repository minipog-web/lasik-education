# Project Rules

- Never deploy to Netlify or GitHub unless explicitly and specifically instructed by the user in their prompt.
- Do NOT run Netlify deployment commands (`npx netlify deploy`, etc.) or push to GitHub after code changes unless the user explicitly requests deployment in that prompt.
